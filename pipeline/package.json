{
  "name": "podsupps-comb-engine",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Autonomous ingestion pipeline for PodSupps — polls podcast feeds, transcribes new episodes, extracts product/restaurant/book mentions with an LLM, resolves them against the catalog, and writes mentions back to the database. Keeps the catalog up to date and expands it to new shows with zero human seeding.",
  "license": "MIT",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "demo": "tsx src/cli.ts demo",
    "poll": "tsx src/cli.ts poll-all",
    "ingest": "tsx src/cli.ts ingest",
    "discover": "tsx src/cli.ts discover",
    "summary": "tsx src/cli.ts summary",
    "qa": "tsx src/qa/agent.ts"
  },
  "dependencies": {
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^24.7.0",
    "tsx": "^4.19.1",
    "typescript": "5.9.3",
    "vitest": "^2.1.4"
  }
}
